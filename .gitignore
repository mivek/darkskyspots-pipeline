# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/
build/
dist/

# Debug rasters (large, regenerated each run, never versioned)
debug_darkness_*.tif
debug_bortle_*.tif

# Pipeline inputs/outputs (large, regenerated each run, never versioned)
# The /input dir holds the VIIRS GeoTIFFs and OSM extracts (large, ephemeral).
# The /spots dir is the local staging area for step 7 to push to the data repo.
/input/
/output/
/spots/

# Design and planning docs (ephemeral, regenerated each feature)
/docs/designs/
/docs/plans/

# Validation fixtures (large)
/validation/*.tif
/validation/*.geojson
/validation/*.pbf

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
*~

# OpenCode session transcripts (dropped in cwd by opencode, never project content)
session-*.md

# Secrets (if any local-only credentials are ever staged)
.env
.env.local
